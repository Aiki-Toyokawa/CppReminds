共用体union (ユーザー定義型)

共用体を宣言するときはunionを使う
構造体と似ている、宣言はほぼ構造体と同じ
共用体のメンバは同時に別の値を記憶することができない
共用体のメンバは同じメモリを共有している→共用体

union 共用体型名 {
   型名　識別子1;          //識別子がメンバ
   型名　識別子2;          //異なる型名で識別子を宣言できる
   ...
};








